# Delilah Brain v2
DATABASE_URL=postgresql://USERNAME:PASSWORD@HOST:5432/DBNAME
QDRANT_URL=http://qdrant:6333
OLLAMA_URL=http://ollama:11434

# Optional tools (do not commit real tokens)
OPENAI_API_KEY=REDACTED
GOOGLE_CLIENT_ID=REDACTED
GOOGLE_CLIENT_SECRET=REDACTED
GOOGLE_REFRESH_TOKEN=REDACTED

# General
LOG_LEVEL=INFO
ENVIRONMENT=production
# =========================
# Feature Flags (Agentic)
# =========================

# Phase 7 agentic retrieval features (default conservative)
HIERARCHICAL_RAG_ENABLED=false
RETRIEVAL_LOOP_ENABLED=false
MAP_REDUCE_ENABLED=false

# Safety / control defaults
MUTATING_TOOLS_ENABLED=false
DRY_RUN_DEFAULT_FOR_MUTATIONS=true
PARALLEL_EXPERT_FANOUT_ENABLED=false

# Provider/runtime (future; enable only when deployed/configured)
VLLM_PROVIDER_ENABLED=false
